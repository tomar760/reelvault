/* ReelVault — CONNECTION CONFIG
   Leave BACKEND_URL empty → DEMO MODE (sample data).
   Fill your Render URL (or set it in Settings page) → LIVE MODE.
   You can also edit this file directly instead of using Settings. */
window.RV_CONFIG = {
  // Aditya's Render backend — ALREADY FILLED (no trailing slash)
  BACKEND_URL: "https://reelvault-9amb.onrender.com",
};
